
function AuthTitle({ title }: { title: string }) {
  return (
            <h2 className="text-white md:text-3xl lg:text-4xl text-3xl   font-semibold text-center mb-2">
              {title}
            </h2>
  )
}

export default AuthTitle
